import { environment } from "src/environments/environment.development";

export const config = {
    URL: environment.URL,
    URL_API: environment.URL_API,
    URL_FRONT: environment.URL_FRONT,
    // URL_API_AUTH: environment.URL_API + '/auth',
    // URL_API_USERS: environment.URL_API + '/users',
    // URL_API_PRODUCTS: environment.URL_API + '/products',
}